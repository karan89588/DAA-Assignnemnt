#include<iostream>
#include<stdlib.h>
using namespace std;
bool counting(int[],int,int);
int main(){
    int *a=NULL,t,n,k=-1;
    cin>>t;
    while(t--){
        cin>>n;
        a=(int*)malloc(sizeof(int)*n);
        for(int i=0;i<n;i++) cin>>a[i];
        for(int i=0;i<n;i++) if(a[i]>k) k=a[i];
        if(counting(a,n,k))	cout<<"Yes";
        else cout<<"No";
    }
    return 0;
}
bool counting(int a[],int n,int k){
	int *c=(int*)malloc(sizeof(int)*(k+1));
	for(int i=0;i<k+1;i++) c[i]=0;
	for(int i=0;i<n;i++) c[a[i]]++;
	for(int i=0;i<n;i++) if(c[i]>=2) return true;
	return false;
}
