#include<iostream>
#include<stdlib.h>
using namespace std;
int binarySearch(int a[],int l,int r,int k,int* c);
int main()
{
    int t,n,*a=NULL,k,r,*c=NULL;
    int count=0;
    c=&count;
    cin>>t;
    while(t--)
    {
        cin>>n;
        a=(int*)malloc(sizeof(int)*n);
        for(int i=0;i<n;i++) cin>>a[i];
        cin>>k;
        r=binarySearch(a,0,n-1,k,c);
        if(r==-1) cout<<"Not Present "<<count;
        else cout<<"Present "<<count;
    }
    return 0;
}
int binarySearch(int a[],int l,int r,int k,int *c)
{
    if(l>r) return -1;
    else
    {
        int m=l+(r-l)/2;
        if(a[m]<k) {
        	(*c)++;
		return binarySearch(a,m+1,r,k,c);
		}
        else if(a[m]>k) {
        	(*c)++;
		return binarySearch(a,l,m-1,k,c);
		}
        else {
        	(*c)++;
		return m;
		}
    }
}
